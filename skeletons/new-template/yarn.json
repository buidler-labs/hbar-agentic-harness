{
  "name": "my-template-yarn",
  "description": "Yarn commands for my-template. Keep install named \"install\" so the harness can skip it across attempts when the lockfile fingerprint is unchanged. Prefer next:build over a separate typecheck when the production build already typechecks.",
  "requiresNoSecrets": true,
  "forbiddenCommands": [
    "npm install",
    "npm run",
    "pnpm install",
    "pnpm run"
  ],
  "commands": [
    {
      "name": "install",
      "command": "yarn install",
      "timeoutMs": 300000,
      "purpose": "Install workspace dependencies."
    },
    {
      "name": "lint",
      "command": "yarn lint",
      "timeoutMs": 180000,
      "purpose": "Run the template lint script."
    },
    {
      "name": "build",
      "command": "yarn next:build",
      "timeoutMs": 300000,
      "purpose": "Production build (includes TypeScript checks for Next.js templates)."
    }
  ]
}
